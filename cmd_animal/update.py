def update(name, engine='default', force=False):
    # check current version of engine

    # if current_version > engine:
    #   'warning: downgrading is not recommended use --force to continue'

    # elif current_version = engine:
    #   f'warning: animal's engine is already {engine}'

    # else:

    # change engine in docker-compose.yml

    return 'not implemented yet'
